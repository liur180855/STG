(function () { 
    angular
        .module('app')
        .controller('postInfo.IndexController', Controller );

	function Controller(dataService) {
        var vm = this;

		
    }
})();
